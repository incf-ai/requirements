# Project overview

A fully featured fixture project exercising every field of the on-disk RON
format: nested submodules, project- and module-level shared attachments and
templates (including a nested attachment path), local and remote
dependencies, the `Submodules` dependency kind, both `FreeForm` and
`Template` result kinds, and all three result statuses.
